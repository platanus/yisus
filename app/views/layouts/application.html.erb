<!DOCTYPE html>
<html>
  <head>
    <title>Yisus</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag "application" %>

    <%= stylesheet_pack_tag 'application' %>
  </head>

  <body>
    <div id="vue-app">
      <% if current_user %>
        <app>
          <div class="container p-4 mx-auto mb-24 sm:px-36 sm:py-8 ">
            <%= yield %>
          </div>
        </app>
      <% else %>
        <%= yield %>
      <% end %>
    </div>
  </body>
</html>
